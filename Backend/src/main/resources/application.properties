spring.application.name=ecommerce

# Configuración de PostgreSQL fuera de docker
spring.datasource.url=jdbc:postgresql://localhost:5432/motionparts
# Configuración de PostgreSQL (para Docker)
#spring.datasource.url=jdbc:postgresql://db:5432/motionparts
spring.datasource.username=postgres
spring.datasource.password=admin
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# Mostrar las consultas SQL en la consola (opcional)
spring.jpa.show-sql=true

# Configuración de JWT
app.jwt.secret=EGMD2sX+K8Zp0MbS00d6B2TCai4qwkRw8V/LJwvHrtI=
app.jwt.expiration=86400000
